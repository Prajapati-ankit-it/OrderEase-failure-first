# API Gateway Configuration Example
# Copy this file to .env and update with your values

# Server Configuration
PORT=4000
BACKEND_URL=http://localhost:3001
ORDER_SERVICE_URL=http://localhost:3002

# CORS Configuration
# Set to '*' to allow all origins (not recommended for production with credentials)
# Or provide comma-separated list of allowed origins
# Examples:
#   CORS_ORIGIN=http://localhost:3000
#   CORS_ORIGIN=http://localhost:3000,https://app.example.com
#   CORS_ORIGIN=*
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# ==========================================
# IP-based Rate Limiting Configuration
# ==========================================

# RATE_LIMIT_TTL: Time window in milliseconds
# Default: 60000 (60 seconds)
# Examples:
#   - 30000 = 30 seconds
#   - 60000 = 1 minute
#   - 300000 = 5 minutes
RATE_LIMIT_TTL=60000

# RATE_LIMIT_MAX: Maximum requests per IP per time window
# Default: 100
# Examples:
#   - 10 = Very strict (good for login/signup endpoints)
#   - 50 = Moderate (good for general API usage)
#   - 100 = Relaxed (good for public endpoints)
#   - 1000 = Very relaxed (good for internal services)
RATE_LIMIT_MAX=100

# ==========================================
# Rate Limiting Presets (uncomment to use)
# ==========================================

# Strict: 30 requests per 30 seconds
# RATE_LIMIT_TTL=30000
# RATE_LIMIT_MAX=30

# Moderate: 50 requests per minute
# RATE_LIMIT_TTL=60000
# RATE_LIMIT_MAX=50

# Relaxed: 200 requests per 2 minutes
# RATE_LIMIT_TTL=120000
# RATE_LIMIT_MAX=200
